// Importar móduos
const http = require('node:http')
const porta = 8001

// criar servidor e rotas 
const server = htto.createServer(function (req, res) {} => {
    // rotas e conteúidos 
})

// liberar porta no meu PC
server.listen(porta, () => {
   console.log (`Servidor rodando em http://localhost:${porta}`
   })